<h1>Edit your Review</h1>

    <%= form_for @group do |f| %>

      <%= f.label "name" %>
      <%= f.text_field :name %>

      <%= f.collection_check_boxes :user_ids, User.all, :id, :name %>

      <%= f.submit "Submit Edits" %>
    <%- end -%>
